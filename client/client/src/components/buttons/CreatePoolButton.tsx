import React from 'react';
import {Button} from "react-bootstrap";
import './buttons.css';

const CreatePoolButton = () => {
    return (
        <div>
            <Button className="col-12 rounded-default">Create pool</Button>
        </div>
    );
};

export default CreatePoolButton;